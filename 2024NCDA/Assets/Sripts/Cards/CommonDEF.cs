using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CommonDEF : Cards
{
    // Start is called before the first frame update
    void Start()
    {
        BaseInit(2,0,2,1);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
